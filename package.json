{
  "name": "div-tilt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:default && npm run build:browser",
    "build:default": "tsc && vite build",
    "build:browser": "tsc && vite build -c vite.browser.config.ts"
  },
  "main": "dist/div-tilt.es.js",
  "exports": {
    ".": "./dist/index.js",
    "./browser": "./dist/browser/div-tilt.iife.js"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
